// Deprecated native GoogleAuth plugin.
// This app now uses a web-based Google sign-in flow, so this
// stub remains only to satisfy any legacy references at build time.

package com.strengthy.app.plugins;

import com.getcapacitor.Plugin;
import com.getcapacitor.annotation.CapacitorPlugin;

@CapacitorPlugin(name = "GoogleAuth")
public class GoogleAuthPlugin extends Plugin {
    // Intentionally left blank.
}
